{
  "name": "budgets-server",
  "version": "0.1.0",
  "private": true,
  "description": "PoC backend for the budgets app: auth, accounts, CSV statement import, categorization, budgets.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "google-auth-library": "^9.14.2",
    "multer": "^2.0.0"
  }
}
